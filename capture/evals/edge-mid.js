(() => { const es = [...document.querySelectorAll('.react-flow__edge')]; const e = es.find((x) => /Text-/.test(x.getAttribute('data-id') || x.getAttribute('data-testid') || '')) || es[es.length - 1]; if (!e) return '0,0'; const p = e.querySelector('path.react-flow__edge-path'); const len = p.getTotalLength(); const pt = p.getPointAtLength(len * 0.5); const svg = p.ownerSVGElement; const m = svg.getScreenCTM(); const sp = svg.createSVGPoint(); sp.x = pt.x; sp.y = pt.y; const s = sp.matrixTransform(m); return Math.round(s.x) + ',' + Math.round(s.y); })()
