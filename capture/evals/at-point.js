(() => { const [x, y] = (window.__pt || '0,0').split(',').map(Number); const el = document.elementFromPoint(x, y); if (!el) return 'none'; const chain = []; let e = el; for (let i = 0; i < 6 && e; i++) { const r = e.getBoundingClientRect(); chain.push({ tag: e.tagName, cls: String(e.className && e.className.baseVal !== undefined ? e.className.baseVal : e.className).slice(0, 70), aria: e.getAttribute('aria-label'), r: [Math.round(r.x), Math.round(r.y), Math.round(r.width), Math.round(r.height)] }); e = e.parentElement; } return chain; })()
