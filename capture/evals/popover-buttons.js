(() => { const p = [...document.querySelectorAll('[class*=popover]')].find((e) => e.getBoundingClientRect().width > 0); if (!p) return 'no popover'; return [...p.querySelectorAll('button,[role=button],[role=option],[role=menuitem]')].filter(b => b.getBoundingClientRect().width > 0).map(b => ({ t: (b.getAttribute('aria-label') || b.title || b.innerText || '').trim().slice(0, 30), r: (r => [Math.round(r.x), Math.round(r.y), Math.round(r.width), Math.round(r.height)])(b.getBoundingClientRect()) })); })()
